import { LighBoxDirective } from './ligh-box.directive';

describe('LighBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new LighBoxDirective();
    expect(directive).toBeTruthy();
  });
});
